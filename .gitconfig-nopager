[core]
    pager = cat

[pager]
    log = cat
    diff = cat
    show = cat
    status = cat
    branch = cat
    tag = cat

[alias]
    # Git commands without paging
    l = log --oneline
    ll = log --oneline --all --graph
    s = status
    d = diff
    sh = show
    st = status
