# uScript

Utility scripts for uDOS data extraction, conversion, and automation.

- /extract — Scripts for extracting and converting datasets (CSV, Markdown, etc.)
- /render  — Scripts for rendering templates with data

