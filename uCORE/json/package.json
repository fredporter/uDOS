{
  "name": "udos-template-system",
  "version": "1.0.4",
  "description": "uDOS Template System with Dataset Integration and Map Generation",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "map": "npm run build && node dist/index.js",
    "map:generate": "npm run build && node dist/index.js generate",
    "map:region": "npm run build && node dist/index.js region",
    "map:city": "npm run build && node dist/index.js city",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "udos",
    "template",
    "mapping",
    "coordinates",
    "dataset",
    "typescript"
  ],
  "author": "uDOS Team",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0"
  },
  "dependencies": {},
  "engines": {
    "node": ">=16.0.0"
  }
}
