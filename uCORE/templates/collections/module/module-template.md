# uDOS Module Template

**Generated**: {TIMESTAMP}  
**Module**: {MODULE_NAME}  
**Type**: {MODULE_TYPE}  
**Author**: {USER}

## Overview
{MODULE_DESCRIPTION}

## Structure
```
{MODULE_NAME}/
├── README.md              # This file
├── {MODULE_NAME}-core.sh  # Core functionality
├── config/                # Configuration files
├── api/                   # API endpoints (if applicable)
├── data/                  # Data storage
└── tests/                 # Test suite
```

## Core Script Template
```bash
#!/bin/bash
# {MODULE_NAME} Core Module v1.0.0
# {MODULE_DESCRIPTION}

set -euo pipefail

# Module configuration
MODULE_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
MODULE_NAME="{MODULE_NAME}"
MODULE_VERSION="1.0.0"

# Initialize module
init_{MODULE_NAME}() {
    echo "🚀 Initializing $MODULE_NAME..."
    
    # Create required directories
    mkdir -p "$MODULE_ROOT/config"
    mkdir -p "$MODULE_ROOT/data"
    mkdir -p "$MODULE_ROOT/logs"
    
    echo "✅ $MODULE_NAME initialized"
}

# Main functionality
{MODULE_NAME}_main() {
    local command="${1:-help}"
    shift || true
    
    case "$command" in
        "init")
            init_{MODULE_NAME}
            ;;
        "status")
            echo "$MODULE_NAME v$MODULE_VERSION - Status: Online"
            ;;
        "help"|"--help"|"-h")
            echo "Usage: $0 {init|status|help}"
            ;;
        *)
            echo "Unknown command: $command"
            exit 1
            ;;
    esac
}

# Execute if run directly
if [[ "${BASH_SOURCE[0]}" == "${0}" ]]; then
    {MODULE_NAME}_main "$@"
fi
```

## Integration Points
- **uCORE**: Core system integration
- **uNETWORK**: {NETWORK_INTEGRATION}
- **uMEMORY**: {MEMORY_INTEGRATION}
- **uKNOWLEDGE**: {KNOWLEDGE_INTEGRATION}

## Configuration
- **Config File**: `config/{MODULE_NAME}.conf`
- **Log Level**: {LOG_LEVEL}
- **Auto-start**: {AUTO_START}

## API Endpoints (if applicable)
- `GET /{MODULE_NAME}/status` - Module status
- `POST /{MODULE_NAME}/action` - Execute action

## Testing
```bash
# Run module tests
./tests/test-{MODULE_NAME}.sh

# Integration test
./tests/integration-test.sh {MODULE_NAME}
```

---
*Generated by uDOS Template Engine v{TEMPLATE_ENGINE_VERSION} on {DATE}*
