services:
  wizard:
    build:
      context: ..
      dockerfile: containers/wizard/Dockerfile
    environment:
      - UDOS_ROOT=/app
      - UDOS_ROOT_REQUIRED=1
      - VAULT_ROOT=/app/memory/vault
    working_dir: /app
    volumes:
      - ../:/app
    ports:
      - "8765:8765"
