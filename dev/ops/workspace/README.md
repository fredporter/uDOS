# @dev Workspace Assets

Canonical contributor workspace assets live in `dev/ops/templates/`.

This directory exists for future editor-specific overlays that should remain part of the tracked `@dev` operations payload.
