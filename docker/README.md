# uOS Docker Launcher

This folder contains the Docker runtime for uOS.

To rebuild and start the uOS shell:
```bash
./Launch-uOS.command
```

Ensure [Docker Desktop](https://www.docker.com/products/docker-desktop) is installed and running.
