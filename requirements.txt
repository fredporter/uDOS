# uDOS v1.2.10 Requirements
# Core dependencies for uDOS operation with flexible versioning

# AI Integration (Gemini)
google-generativeai>=0.3.0

# Gmail & Google Drive Integration (v1.2.9+)
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1
google-api-python-client>=2.100.0

# Image Processing & Vectorization (NEW v2.0)
Pillow>=10.0.0
vtracer>=0.6.0

# Environment variable management
python-dotenv>=1.0.0

# Timezone detection (v1.2.25+)
tzlocal>=4.0

# Interactive CLI with tab completion
prompt_toolkit>=3.0.0

# JSON Schema validation (for checklist system)
jsonschema>=4.0.0

# YAML parsing (for prompt templates)
PyYAML>=6.0.0

# Encryption for private commands
cryptography>=41.0.0

# HTTP requests for web extensions
requests>=2.31.0

# HTML parsing for website scraping (BIZINTEL)
beautifulsoup4>=4.12.0

# System information (recommended for health checks)
psutil>=5.9.0

# Web Extensions (markdown-viewer, knowledge browser)
flask>=2.0.0
markdown2>=2.4.0
pygments>=2.10.0
thefuzz>=0.19.0

# Webhook Integration System (v1.2.10+)
flask-cors>=5.0.0
aiohttp>=3.11.0
hypercorn>=0.18.0

# TUI Enhancement (v1.2.8+)
rich>=13.0.0

# Timezone Support (v1.2.22+)
pytz>=2023.3

# Password Hashing (v1.2.22+)
bcrypt>=4.0.0

# Testing
pytest-asyncio>=0.23.0

# Optional: File Watching (for hot-reload)
# watchdog>=3.0.0

# Optional: Tree Display (enhanced TREE command)
# tree>=0.1.0

# Notes:
# - All packages use minimum version constraints (>=) for flexibility
# - Python 3.8+ required (3.10+ recommended)
# - Python 3.9 will work but is EOL (October 2025)
# - Node.js and npm required for typo editor (separate installation)
# - Monaspace fonts are optional (see extensions/setup_monaspace.sh)
# - Web extensions require modern browser (Chrome, Firefox, Safari)

# Installation:
#   pip install -r requirements.txt
# Or with upgrade:
#   pip install --upgrade -r requirements.txt

# Health Check:
#   python3 core/uDOS_health.py
# Or from CLI:
#   python3 -c "from core.uDOS_health import run_quick_check; run_quick_check()"
